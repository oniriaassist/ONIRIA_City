# Vercel + Supabase Deployment

The supported production target is Supabase PostgreSQL plus two Vercel projects (`backend` and `frontend`).

See `docs/PRODUCTION_DEPLOYMENT.md` for the complete current procedure.
